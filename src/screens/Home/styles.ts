import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
  },
  logo: {
    width: 180,
    height: 100,
    marginTop: 50,
    marginBottom: 20,
    
  },
  contentList: {
    paddingLeft: 32,
    paddingRight: 60,
  }
});